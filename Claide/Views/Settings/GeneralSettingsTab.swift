// ABOUTME: General settings tab for shell, scrollback, and tab behavior preferences.
// ABOUTME: Placeholder for future settings as the app grows.

import SwiftUI

struct GeneralSettingsTab: View {
    var body: some View {
        Form {
            Section("General") {
                Text("More settings coming soon.")
                    .foregroundStyle(.secondary)
            }
        }
        .formStyle(.grouped)
        .frame(width: 380)
    }
}
